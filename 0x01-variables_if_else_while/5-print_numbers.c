#include <stdio.h>

/**
 * main - Start of a program
 * Description : To print digit single digit
 * Return:0
 */
int main(void)
{
	int digit;

	for (digit = 0; digit < 10; digit++)
	{
	 printf("%d", digit);
	}
	printf("\n");
	return (0);
}
